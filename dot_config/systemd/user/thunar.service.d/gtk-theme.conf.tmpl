# Route the D-Bus/systemd-activated Thunar daemon through the wrapper that
# applies the Ghostty-TokyoNight GTK theme. See ~/.themes/Ghostty-TokyoNight/README.md
[Service]
ExecStart=
ExecStart={{ .chezmoi.homeDir }}/.local/bin/thunar --daemon
