# Real commands the cleaner cannot resolve on PATH -- uninstalled, or living
# only inside a virtualenv or on another machine. One per line; blank lines and
# # comments ignored.

deactivate      # virtualenv teardown function, only defined while a venv is active
helix           # config lives in dot_config/helix; binary is `hx`, or not installed here
obsidian        # AppImage / flatpak, not on PATH
tio             # serial terminal, not currently installed
tty-clock
xfce4-panel
xournalpp
