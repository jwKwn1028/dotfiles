/* One look for every mode: spotlight.rasi carries theme, font, and matching, so
   `rofi -show window` does not fall back to the stock theme. */
configuration {
  modi: "drun,run,window";
  show-icons: true;
  drun-display-format: "{name}";
}
@theme "spotlight"
